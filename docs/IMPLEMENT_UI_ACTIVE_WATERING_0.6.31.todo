IMPLEMENT IN custom_components/nikas_ho_sc_8w/frontend/irrigation-panel.js

1. Bump UI_VERSION 0.6.30 -> 0.6.31 and matching panel metadata.
2. Add helper that returns active zone from Active zones entity and live remaining minutes from e.zones[zone].remaining.
3. systemStatus(e):
   - Auto + active: title `Автополив · зона N`, sub `Осталось N мин` when remaining > 0, otherwise `Полив выполняется`.
   - Manual + active: title `Ручной полив · зона N`, same remaining line.
   - Do not use generic `Контроллер выполняет программу` while runtime is available.
4. zoneRuntime(e, zone): active card gets running tone and live label `Полив · осталось N мин` when remaining > 0.
5. Active zone visual treatment: light-blue full card background, blue outline, active water path; preserve readable contrast.
6. zonesView(e): remove eyebrow `ЗОНЫ 1–6` and heading `Рабочие зоны`; retain only enlarged explanatory text `Фактическое состояние и программа каждого канала.`
7. At 100% scale make Zones view a work-area-height layout: explanation + six equal flexible rows filling available space between fixed Header and Bottom Nav. Avoid vertical scroll on the approved iPhone viewport; do not reduce established typography.
8. Keep program duration distinct from live remaining runtime.
9. Update CI/UI contract assertions for UI 0.6.31 and the new Zones intro/layout markers.

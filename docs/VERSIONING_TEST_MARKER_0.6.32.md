# UI 0.6.32 / integration 1.0.0-b005.50

Field-test marker for manual DP45 native-semantics validation.

- UI header must show `UI v0.6.32`.
- Installed integration must report `1.0.0-b005.50`.
- Manual Stop uses DP45 reset semantics, not DP101 Auto/OFF.
- If the screen still shows 0.6.30/0.6.31 or the old DP101/107/108 Auto error, Home Assistant is running stale integration files.
